const mime = require('mime-types');

app.get('/files/:id/getShow', (req, res) => {
    return files;
});

app.get('/files/getIndex', (req, res) => {
    return files;
});

app.get('/files/:id/data', (req, res) => {
    return files;
});

app.post('/files/image', (req, res) => {
    return files;
});